diff --git a/dist/commonjs/index.js b/dist/commonjs/index.js
--- a/dist/commonjs/index.js
+++ b/dist/commonjs/index.js
@@ -280,10 +280,17 @@ function expand_(str, max, maxLength, isTop) {
             }
         }
         acc = combine(acc, pre, values, max, maxLength, dropEmpties && !m.post.length);
         if (!m.post.length)
             break;
         str = m.post;
     }
     return acc;
 }
+// Preserve the callable CommonJS export used by minimatch 3 while retaining
+// brace-expansion 5's named exports and security limits.
+module.exports = Object.assign(expand, {
+    expand,
+    EXPANSION_MAX: exports.EXPANSION_MAX,
+    EXPANSION_MAX_LENGTH: exports.EXPANSION_MAX_LENGTH,
+});
 //# sourceMappingURL=index.js.map
